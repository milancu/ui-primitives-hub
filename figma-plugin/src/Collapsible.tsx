import * as React from 'react';
import {Frame, View} from 'react-figma';

export const Collapsible = () => {
    return (
        <Frame>
            <View>
                <View style={{width: 200, height: 100, backgroundColor: '#55ddb9'}}/>
            </View>
        </Frame>
    );
};
